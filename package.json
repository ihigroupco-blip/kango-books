{
  "name": "kango-books",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && NODE_ENV=production node src/index.js",
    "migrate": "cd backend && npx prisma migrate deploy",
    "postinstall": "npm run install:all && npm run migrate && npm run build"
  }
}
